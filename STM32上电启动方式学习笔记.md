# STM32 上电启动方式有几种及问题排查思路

详细解析

[STM32启动流程详解（超全，startup_stm32xx.s分析）_stm32启动过程详解-CSDN博客](https://blog.csdn.net/m0_71284871/article/details/148875446)

## STM32 上电启动方式

STM32 通过 **BOOT0、BOOT1 (部分型号)** 引脚电平，决定上电后从哪块存储区域启动，一共**3 种启动模式**。

> 注意：不同型号命名略有区别：
>
> - F1：BOOT0、BOOT1
> - G4/F4/H7：只有 BOOT0，BOOT1 不再是引脚，由选项字节配置

## 三种启动模式

| 启动模式             | BOOT0 | BOOT1 (仅 F1) | 启动地址    | 说明                                                         |
| -------------------- | ----- | ------------- | ----------- | ------------------------------------------------------------ |
| 主闪存启动（最常用） | 0     | 任意          | 0x0800 0000 | 从片内 Flash 启动，正常运行用户程序，产品实际使用模式        |
| 系统存储器启动       | 1     | 0             | 0x1FFF 0000 | 芯片出厂内置 ISP BOOT，串口下载模式，用于串口烧录程序，不能存用户代码 |
| SRAM 启动            | 1     | 1             | 0x2000 0000 | 从内部 RAM 启动，用于调试，RAM 掉电丢失，不能上电直接跑用户固件 |

### 1. 主闪存启动 Main Flash memory（BOOT0=0）

- BOOT0 引脚拉低，不管 BOOT1 是什么状态。
- CPU 把**Flash 的 0x08000000 映射到 0x00000000（启动向量表地址）**，复位后从此取中断向量，运行我们烧写的程序。

> 绝大多数产品，BOOT0 直接接地，上电直接跑 Flash 程序。

### 2. 系统存储器启动 System memory（BOOT0=1，BOOT1=0，F1）

- 运行 ST 出厂固化在芯片内部的 ISP 引导代码，**这部分 ROM 用户不能擦写**。
- 功能：USART 串口下载程序，不需要仿真器，串口即可烧录 Flash。
- 烧录完成后，要把 BOOT0 拉低，重新上电，才能运行刚烧进去的用户程序。

### 3. 内置 SRAM 启动 SRAM（BOOT0=1，BOOT1=1，F1）

- 将 SRAM 映射到启动地址 0x00000000。
- 用途：调试，把程序下载到 RAM 调试，**掉电程序消失，不能作为产品启动**。
- 开发场景：调试阶段，不想反复擦写 Flash，把向量表放到 RAM，直接在 RAM 运行代码。

## 重要补充

1. **G4/F4/H7 没有 BOOT1 硬件引脚**
   - 只看 BOOT0 引脚：
     - BOOT0=0 → Flash 启动
     - BOOT0=1 → 系统存储器启动
   - SRAM 启动需要修改**选项字节 (Option bytes)**，软件配置，不是引脚控制。
2. 复位后，**只有启动瞬间采样 BOOT 引脚电平**，程序运行后再改变 BOOT 引脚电平，不会改变当前运行存储器。必须重新复位 / 上电才生效。
3. 向量表重映射：启动之后，代码内部可以修改`SCB->VTOR`，修改向量表位置，只是**改变中断向量，不等于改变启动模式**。

## 问题排查思路

### 第一大类：BOOT 引脚配置错误（最高概率）

1. BOOT0 硬件是高电平（一直接 3.3V）

   - 上电进入系统存储器 ISP 模式，芯片等待串口下载，**不会运行 Flash 里的用户程序**。

   - 现象：仿真器连接时可以调试运行；拔掉仿真器上电无反应。

     解决：BOOT0 下拉到 GND，复位重新上电。

2. BOOT 引脚悬空，电平受干扰，随机启动。

   解决：必须加 10K 下拉，不能悬空。

### 第二大类：复位电路问题

1. NRST 复位引脚悬空；复位电阻电容缺失。

2. NRST 被外部电路持续拉低，芯片一直处于复位状态。

   测量：上电用万用表测 NRST 引脚，正常应该是 3.3V；如果一直 0V，查复位电路。

### 第三大类：芯片选项字节 Option Bytes 配置错误

常见：读保护开启、硬件 BOOT 选择被改写。

1. 开启读保护 (RDP)：下载正常，退出仿真器不运行。

   处理：ST‑Link Utility → Option Bytes，关闭读保护 RDP，重新下载。

> ⚠关闭读保护会**整片 Flash 擦除**。

1. G4/F4/H7 选项字节强制设置为 SRAM 启动，忽略 BOOT0 引脚。

   打开 ST‑Link Utility 查看选项字节 BOOT 配置，改回从 Flash 启动。

### 第四大类：工程配置问题（仿真能跑，脱机不行）

1. Keil 下载算法选错，没有真正烧写到 Flash，只是下载到 RAM 调试。

> Target 选项卡：看 Flash 大小配置；Download 里选对 Flash 下载算法。
>
> 如果是 RAM 调试配置，只接仿真器能跑，断电程序丢失。

1. 向量表偏移配置错误 `SCB->VTOR`

- 程序里写死 VTOR 偏移，但是实际烧录的 Flash 地址不对。

> 正常从 0x08000000 启动，VTOR 默认就是 0，不需要手动修改；如果做 OTA/APP+IAP，才需要修改 VTOR。

1. 编译生成的 hex/bin 文件不对，烧录地址偏移。

   验证：读取芯片 Flash，对比 hex 文件，确认 0x08000000 位置有正确向量表（前 4 个字是栈顶、复位中断地址）。

### 第五大类：硬件电源问题

1. 上电瞬间供电不稳，电压跌落，仿真器供电可以，板子独立供电电压不足。
2. VCAP 引脚（内核电容）没有接 100nF 电容，很多 STM32 会出现脱机无法启动。

> G4/F1 都有 VCAP 引脚，必须接 0.1uF 到 GND，不能悬空。

### 第六大类：软件代码卡死

上电直接进入 HardFault，仿真器一接上就复位正常。

- 上电初始化时操作非法地址、时钟配置错误、外部外设初始化时序错误直接死机。

  排查：在 main 函数最开头点亮 LED，看上电 LED 是否闪烁；如果 main 第一行都跑不到，基本不是业务代码问题，优先查硬件 / BOOT / 选项字节。

------

## 快速排查流程（实操顺序）

1. 万用表测 BOOT0 上电电平：必须是**低电平 0V**；高电平就直接找到问题。
2. 测 NRST 复位引脚，上电后是 3.3V，不能持续低。
3. 打开 ST‑Link Utility 读取 Option Bytes，关闭读保护 RDP。
4. 确认 Keil 不是 RAM 调试模式，下载算法为 Flash。
5. 检查 VCAP 内核电容是否焊接。
6. 仿真器在线调试，复位，看能否进入 main 函数第一行。